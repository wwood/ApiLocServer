<h1>Listing clustersets</h1>

<table>
  <tr>
  </tr>

<% for clusterset in @clustersets %>
  <tr>
    <td><%= link_to 'Show', clusterset %></td>
    <td><%= link_to 'Edit', edit_clusterset_path(clusterset) %></td>
    <td><%= link_to 'Destroy', clusterset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New clusterset', new_clusterset_path %>
